const nome = 'Luiz';    // string
const nome1 = "Luiz";   // string
const nome2 = `Luiz`;   // string
const num1 = 10;    // number
const num2 = 10.52; // number

let nomeAluno;  // undefined = nao aponta pra local nenhum na memoria (declaramos e nao colocamos valor nenhum)
let sobrenomeAluno = null; // Nulo -> nao aponta para local nenhum na memoria

const aprovado = true; // False  =   Valor Boolean (valor logico)

let a = 2;
let b = a;
console.log(a, b);

a = 3;
console.log(a, b)